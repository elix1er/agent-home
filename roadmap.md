# Roadmap

The template starts deliberately narrow: portable task operations, explicit capability grants, evidence-based completion, and an optional user-owned MCP.

Possible later work belongs in issues and should earn its way into the product through real use. V1 intentionally excludes a central service, multi-tenant database, arbitrary executor, and npm CLI.
